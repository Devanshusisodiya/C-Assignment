#include <stdio.h>

int main() {

    char c; //initialising character variable

    for (c = 'A'; c <= 'Z'; c++) //running the loop to print the letters
        printf("%c ", c);

    return 0;
}
