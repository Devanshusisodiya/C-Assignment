#include <stdio.h>

int main(){

  int n = 10;   //initialising n

  //running for loop for printing values between a range which is 10-1
  for(int i = n; i > 0; i--){
    printf("%d\n", i);
  }

  return 0;
}
